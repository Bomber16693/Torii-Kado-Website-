# Publishing

Upload the entire folder without changing the directory structure.

The website has no build step and no server-side dependencies.

Entry page: `index.html`

For a single-file preview, use `Torii-Kado-Landing-Page.html`.
